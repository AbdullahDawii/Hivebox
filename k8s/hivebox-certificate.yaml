apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hivebox-tls
  namespace: default
spec:
  secretName: hivebox-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - hivebox.local
